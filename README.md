# Fund-Me Frontend

Material UI 3-style React workspace for the Fund-Me contribution management system.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_URL` if the backend is not on `http://localhost:5000/api`.
